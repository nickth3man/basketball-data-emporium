# Generated artifacts — regenerated by `npm run gen:types` and
# `npm run gen:sse-schema`. Kept TRACKED (the CI drift guard in
# .github/workflows/chat.yml diffs sse-events.schema.json, and the
# sse-events.drift test imports it), so they must stay in git — just
# excluded from Prettier's format check.
/src/generated/
